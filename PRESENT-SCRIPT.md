# สคริปต์นำเสนอ (พูดคนเดียว ไม่มีกรรมการถาม) — เป้าหมาย ≤ 10 นาที

ใช้พูดตามได้เลย หรืออ่านให้เข้าใจแล้วพูดด้วยคำพูดตัวเอง (แนะนำแบบหลัง จะดูเป็นธรรมชาติกว่า)
เวลาที่กำกับไว้เป็นแค่ไกด์คร่าวๆ พูดเร็ว-ช้ากว่านี้ได้ ขอแค่รวมไม่เกิน 10 นาที

**ทุกจุดที่มี 📂 คือบอกว่าตอนพูดประโยคนั้น ต้องเปิด/ชี้ไฟล์-บรรทัดไหนอยู่บนจอ**
แนะนำเปิดไฟล์ทั้งหมดเป็นแท็บ VS Code เตรียมไว้ล่วงหน้าตามลำดับนี้ก่อนกดอัด จะได้แค่คลิกสลับแท็บ ไม่ต้องเสิร์ชหาสดๆ


เทคโนโลยีหลักที่ใช้ (รู้ไว้เผื่อกรรมการถาม)

Gin — framework สำหรับสร้าง web server ภาษา Go (คล้ายๆ Express ของ Node.js)
GORM — ตัวช่วยคุยกับฐานข้อมูลโดยไม่ต้องเขียน SQL ดิบทุกบรรทัด
Postgres — ฐานข้อมูลหลัก เก็บสูตรอาหาร ผู้ใช้ คะแนน ฯลฯ
Redis — ที่เก็บข้อมูลชั่วคราวสำหรับตอนล็อกอิน (จะอธิบายด้านล่าง)
Keycloak — ระบบล็อกอินสำเร็จรูปแยกต่างหาก (แอปเราไม่เก็บรหัสผ่านเอง ให้ Keycloak จัดการแทน)
goose — เครื่องมือจัดการการเปลี่ยนโครงสร้างฐานข้อมูลทีละขั้น

---

## [0:00 – 1:00] เปิด — ภาพรวมโปรเจกต์ + จะพูดอะไร

📂 เปิด `cmd/api/main.go` ค้างไว้เป็นภาพรวม (ไม่ต้องชี้บรรทัดเฉพาะ แค่โชว์ให้เห็นว่ามีไฟล์นี้)

"โปรเจกต์นี้ผมโคลนมาจากของอาจารย์ เป็น REST API สูตรอาหาร เขียนด้วย Go ใช้ framework Gin ต่อกับฐานข้อมูล Postgres ผ่าน GORM และมีระบบล็อกอินแยกไปใช้ Keycloak โครงสร้างโค้ดแบ่งเป็น 3 เรื่องหลักคือ `auth` (ล็อกอิน/ล็อกเอาท์), `user` (ข้อมูลผู้ใช้), และ `recipe` (สูตรอาหาร) แต่ละเรื่องแบ่งเป็น 3 ชั้นตามหน้าที่คือ handler รับ request, service เก็บกฎทางธุรกิจ, และ repository คุยกับฐานข้อมูล"

"ของเดิมจากอาจารย์มีแค่ ดูรายการสูตร ดูสูตรเดี่ยว และสร้างสูตร ส่วนที่ผมต่อยอดเพิ่มเข้าไปคือ 2 ฟีเจอร์ในโดเมน `recipe`: **กดโปรด (Favorite)** กับ **ให้คะแนน (Rating)** วันนี้ผมจะอธิบายว่าโค้ดสองส่วนนี้ทำงานยังไงตั้งแต่ request เข้ามาจนตอบกลับ แล้วพูดถึงจุดที่ต้องระวังเป็นพิเศษ 2-3 จุดที่ถ้าเขียนพลาดจะมีบั๊กแอบแฝง"

---

## [1:00 – 3:30] ฟีเจอร์ Favorite

**พูดถึง endpoint ก่อน:**

📂 `internal/recipe/handler.go` — เลื่อนไปดูฟังก์ชัน `Favorite`, `Unfavorite`, `GetRecipes` (ดูชื่อฟังก์ชัน + comment godoc ด้านบนที่มี `@Router`)

"มี 3 จุด: กดโปรด (`POST /recipes/:id/favorite`), เลิกโปรด (`DELETE /recipes/:id/favorite`), และตอนดึงรายการสูตรอาหาร (`GET /recipes`) จะกรองได้ด้วย query `favorite=true` หรือ `false`"

**พูดถึง data flow (เส้นทางของ request):**

📂 ยังอยู่ที่ `internal/recipe/handler.go` ฟังก์ชัน `Favorite` — ชี้ให้เห็นบรรทัดที่เรียก `hdr.service.Favorite(...)`

"เวลากดโปรด request จะเข้า handler ก่อน → handler เช็คว่า login อยู่ไหม (ดึง user id จาก token) → ส่งต่อให้ service → service เรียก repository ไปเขียนลง database ตาราง `user_favorites`"

**พูดถึงจุดที่ต้องระวัง (แบบง่าย ไม่ใช้ศัพท์เทคนิคหนักๆ):**

📂 **`internal/recipe/repository.go` บรรทัด 150–163** — ฟังก์ชัน `Favorite(ctx, userID, recipeID)` ชี้ตรง `Clauses(clause.OnConflict{...})` ← **จุดสำคัญที่สุดของฟีเจอร์นี้ เตรียม cursor/เมาส์ชี้ไว้ตรงนี้เลย**

"จุดที่ผมต้องระวังเป็นพิเศษคือ ถ้าผู้ใช้กดโปรด แล้วกดเลิก แล้วกดโปรดใหม่อีกรอบ — ถ้าเขียนโค้ดแบบเพิ่มแถวใหม่ตรงๆ ทุกครั้ง จะเจอปัญหาแถวซ้ำในฐานข้อมูลทันที เพราะระบบไม่ได้ลบแถวเดิมจริงๆ แค่ทำเครื่องหมายว่า 'ถูกลบแล้ว' ไว้ (เพื่อเก็บประวัติ) ผมเลยแก้โดยสั่ง database ว่า 'ถ้าเจอแถวเดิมของคู่ user กับสูตรนี้อยู่แล้ว ให้ชุบชีวิตแถวเดิมกลับมาแทนที่จะสร้างแถวใหม่' วิธีนี้ทำให้ไม่มีทางเกิดแถวซ้ำเลย ต่อให้ผู้ใช้กดรัวๆ พร้อมกันหลายครั้งก็ตาม"

**พูดถึงตัวกรอง favorite=true/false:**

📂 `internal/recipe/dto.go` **บรรทัด 93–101** — struct `GetRecipesQuery` ชี้ตรง field `Favorite *bool`

"ส่วนตอนกรองรายการ ผมใช้ตัวแปรชนิดพิเศษที่แยกได้ 3 สถานะ คือ 'ไม่ส่ง query มา' กับ 'ส่งมาเป็น true' กับ 'ส่งมาเป็น false' ถ้าใช้ตัวแปร boolean ธรรมดา จะแยกไม่ออกระหว่าง 'ไม่ได้ตั้งใจกรอง' กับ 'ตั้งใจกรองเอาเฉพาะที่ยังไม่กด' ซึ่งจะทำให้ผลลัพธ์ผิด"

📂 ต่อด้วย `internal/recipe/repository.go` **บรรทัด 92–108** — ฟังก์ชัน `List(...)` ชี้ตรง comment `// กับดัก T3: ใช้ Subquery เพื่อกรอง Favorite ไม่ใช่ JOIN` (บรรทัด ~101-108) — ถ้ามีเวลาเหลือค่อยพูดเพิ่มว่าทำไมใช้ subquery แทน JOIN

---

## [3:30 – 6:30] ฟีเจอร์ Rating

**พูดถึง endpoint:**

📂 `internal/recipe/handler.go` — เลื่อนไปฟังก์ชัน `Rate` (มี godoc comment `@Router /recipes/{id}/rating [post]` อยู่ด้านบน)

"ให้คะแนนมี endpoint เดียวคือ `POST /recipes/:id/rating` ให้คะแนน 1-5 ให้ซ้ำได้ ค่าล่าสุดจะแทนที่ค่าเดิม แล้วระบบจะคำนวณคะแนนเฉลี่ยใหม่ทันที ค่าเฉลี่ยนี้จะโผล่ในทั้งหน้ารายการสูตรอาหารและหน้ารายละเอียดสูตร เป็น field ชื่อ `averageRating`"

**พูดจุดคำนวณ averageRating ให้ชัด (คำถามที่โจทย์เน้นว่าต้องตอบได้):**

📂 **`internal/recipe/repository.go` บรรทัด 171–208** — ฟังก์ชัน `Rate(ctx, userID, recipeID, score)` ทั้งฟังก์ชัน ← **ไฟล์/จุดสำคัญที่สุดของทั้งวิดีโอ เปิดค้างไว้นานหน่อยตรงนี้**

"จุดที่คำนวณค่าเฉลี่ยจริงๆ อยู่ในขั้นตอนเดียวกับตอนบันทึกคะแนน คือพอมีคนให้คะแนน ระบบจะ (1) บันทึก/แทนที่คะแนนของคนนั้น (2) คำนวณค่าเฉลี่ยใหม่จากคะแนนทุกคนของสูตรนี้ (3) เขียนค่าเฉลี่ยใหม่กลับไปเก็บไว้ที่ตัวสูตรอาหารเลย เพื่อให้ตอนอ่านข้อมูลไม่ต้องมานั่งคำนวณใหม่ทุกครั้ง อ่านเร็วขึ้น"

**พูดจุดที่ต้องระวัง (ใช้คำง่ายๆ แทนคำว่า transaction/race condition):**

📂 ชี้ตรง **บรรทัด 173**: `repo.db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {` — เส้นเปิด transaction ที่ห่อทั้ง 3 ขั้นตอนไว้

"3 ขั้นตอนนี้ผมมัดให้เป็นก้อนเดียวกัน ทำสำเร็จพร้อมกันหมดหรือไม่ทำเลย เหตุผลคือถ้าแยกทำเป็นคนละขั้นตอน แล้วบังเอิญมี 2 คนให้คะแนนพร้อมกันเป๊ะๆ อาจเกิดเคสที่คนที่สองคำนวณค่าเฉลี่ยไปก่อนที่คะแนนของคนแรกจะถูกบันทึกเสร็จ ผลคือค่าเฉลี่ยที่เก็บไว้จะผิดค้างอยู่ถาวร ไม่มีใครมาแก้ให้อัตโนมัติ"

**พูดเรื่องคะแนนซ้ำ (trap สำคัญอีกจุด):**

📂 ชี้ตรง **บรรทัด 176**: `err := tx.Clauses(clause.OnConflict{...})` — จุด upsert คะแนน (โครงสร้างเดียวกับ Favorite ที่พูดไปก่อนหน้า)

"อีกจุดคือถ้า user คนเดิมให้คะแนนซ้ำ เช่นให้ 4 แล้วเปลี่ยนใจให้ใหม่เป็น 2 ระบบต้อง 'แทนที่' คะแนนเก่าด้วยคะแนนใหม่ ไม่ใช่เก็บทั้งสองค่าไว้แล้วเอามาเฉลี่ยกัน เพราะงั้น ผลลัพธ์สุดท้ายต้องเป็น 2 ไม่ใช่ 3 ที่มาจากการเฉลี่ย (4+2)/2"

**พูดเรื่อง COALESCE แบบง่าย:**

📂 ชี้ตรง **บรรทัด 195**: `Select("COALESCE(AVG(score), 0)")`

"อีกจุดเล็กๆ คือถ้าสูตรอาหารยังไม่มีใครให้คะแนนเลย ค่าเฉลี่ยที่คำนวณได้จากฐานข้อมูลจะเป็นค่าว่าง ไม่ใช่เลข 0 ผมเลยต้องสั่งให้แปลงค่าว่างนั้นเป็น 0 ไว้ล่วงหน้า ไม่งั้นโปรแกรมจะ error ตอนแปลงค่าว่างให้เป็นตัวเลข"

---

## [6:30 – 8:00] เรื่องการล็อกอิน/สิทธิ์เข้าถึง

📂 `internal/middleware/jwt.go` — เปิดทั้งไฟล์ (สั้นแค่ ~50 บรรทัด) ชี้ตรงบรรทัดที่มี `verifier.Verify(ctx.Request.Context(), rawToken)` และ comment ด้านบนที่อธิบายเรื่อง access token vs id token

"ทุก endpoint ของ favorite กับ rating ต้อง login ก่อนถึงจะใช้ได้ ผมเช็คตรงนี้ด้วย middleware ตัวเดียวที่ครอบทุก route ไว้ — พอ request เข้ามา มันจะดึง token จาก header มาตรวจสอบลายเซ็นก่อนว่าถูกออกโดย Keycloak จริงไหม ถ้า token ปลอมหรือไม่มี token เลย จะตอบกลับ 401 ทันที ไม่ปล่อยให้ไปถึงส่วนอื่นของโค้ดเลย"

📂 สลับไป `cmd/api/main.go` ประมาณ **บรรทัด 132–141** — ชี้ตรง `recipeGroup.Use(middleware.JWT(accessTokenVerifier, userService))`

"จุดที่น่าสนใจคือ token ที่ frontend ส่งมาเป็นคนละแบบกับ token ตอน login เลยต้องใช้ตัวตรวจสอบที่ตั้งค่าต่างกันเล็กน้อย แต่ยังคงตรวจลายเซ็นและวันหมดอายุจริงเหมือนเดิมทุกอย่าง ไม่ได้ลดความปลอดภัยลง"

---

## [8:00 – 9:30] เรื่องการทดสอบ (tests) — ตัดได้ก่อนถ้าเวลาไม่พอ

📂 `internal/recipe/service_test.go` — เปิดโชว์เคสไหนก็ได้ 1 เคส (เช่น เคสที่เช็คว่าสูตรไม่มีจริงแล้วต้องไม่เรียก repository)

"ผมเขียนเทสไว้ 2 แบบ แบบแรกคือเทสแบบจำลอง ไม่แตะฐานข้อมูลจริง รันเร็ว ใช้พิสูจน์ว่ากฎทางธุรกิจถูกต้อง เช่น 'ถ้าสูตรอาหารไม่มีอยู่จริง ต้องไม่ไปยุ่งกับฐานข้อมูลเลย'"

📂 สลับไป `internal/recipe/repository_test.go` — เปิดโชว์เคส `TestRepository_Rate_UpsertsAndRecomputesAverage`

"แบบที่สองคือเทสที่ยกฐานข้อมูล Postgres จริงขึ้นมาทดสอบ ใช้พิสูจน์ว่าคำสั่ง SQL ที่เขียนทำงานถูกกับฐานข้อมูลจริง เช่นเคสกดโปรด-เลิก-กดใหม่ต้องเหลือแถวเดียว ซึ่งเทสแบบจำลองอย่างเดียวจับปัญหานี้ไม่ได้ ต้องมีทั้งสองแบบเพราะพิสูจน์คนละเรื่องกัน"

---

## [9:30 – 10:00] ปิดท้าย

📂 ไม่ต้องเปิดไฟล์

"สรุปคือ ผมเพิ่มฟีเจอร์กดโปรดกับให้คะแนนครบตามที่โจทย์กำหนด พร้อมจัดการเคสที่มักพลาดกัน เช่น การกดซ้ำ การให้คะแนนซ้ำ และการคำนวณค่าเฉลี่ยให้ถูกต้องแม่นยำ ขอบคุณครับ"

---

## เผื่อพูดเกินเวลา — ตัดตรงไหนได้บ้าง

สคริปต์นี้เต็มๆ อยู่ที่ ~10:00 พอดี แทบไม่มีที่เผื่อเลย ถ้าซ้อมแล้วเกิน ให้ตัดตามลำดับนี้ก่อน (เรียงจากตัดได้ก่อนไปหลัง):
1. ส่วน "เรื่องการทดสอบ" [8:00–9:30] — ตัดทิ้งได้เกือบหมด เหลือแค่ 1-2 ประโยคสรุป ไม่ต้องเปิดไฟล์เทสเลยก็ได้ (ประหยัดได้เกือบ 1 นาทีครึ่ง)
2. ส่วนภาพรวมโครงสร้างโปรเจกต์ [0:00–1:00] — ตัดพาร์ทอธิบาย 3 ชั้น handler-service-repository ออก เหลือแค่บอกว่าแอปทำอะไรกับเพิ่มฟีเจอร์อะไร (ประหยัดได้ ~20 วิ)
3. ส่วนอธิบาย `COALESCE` (repository.go บรรทัด 195) — พูดสั้นลงเหลือประโยคเดียว ไม่ต้องชี้บรรทัดก็ได้
4. ส่วนตัวกรอง favorite=true/false (dto.go + repository.go List) — พูดสั้นลง ข้าม subquery detail ไปได้

**ห้ามตัด** (3 จุดนี้คือแก่นของฟีเจอร์ที่โจทย์ให้ทำ ต้องมีไฟล์เปิดโชว์เสมอ):
- `repository.go` บรรทัด 150–163 (Favorite / ON CONFLICT)
- `repository.go` บรรทัด 171–208 ทั้งฟังก์ชัน (Rate / คำนวณ averageRating / transaction / คะแนนซ้ำ)
- `middleware/jwt.go` (การเช็ค token)

## สารบัญไฟล์ทั้งหมดที่ใช้ (เรียงตามลำดับเปิดในคลิป)

1. `cmd/api/main.go` (เปิดค้างไว้ตอนเปิดคลิป เป็นภาพรวม)
2. `internal/recipe/handler.go`
3. `internal/recipe/repository.go` บรรทัด 150–163 (Favorite)
4. `internal/recipe/dto.go` บรรทัด 93–101 (GetRecipesQuery)
5. `internal/recipe/repository.go` บรรทัด 92–108 (List / subquery T3)
6. `internal/recipe/repository.go` บรรทัด 171–208 (Rate — จุดหลักของคลิป)
7. `internal/middleware/jwt.go`
8. `cmd/api/main.go` บรรทัด 132–141 (route + middleware)
9. `internal/recipe/service_test.go` (ถ้ามีเวลา)
10. `internal/recipe/repository_test.go` (ถ้ามีเวลา)
